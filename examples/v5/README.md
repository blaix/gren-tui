To run an example, [install](https://gren-lang.org/install/) gren 0.6.x, then:


```
cd [example]
gren run Main
```

Most examples exit on their own, exit with Esc, or tell you how to exit.
Otherwise, you can just Ctrl-C to exit any gren-tui app.
